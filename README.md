# Archi-Logicielle

run on root project
```
npx turbo run dev
```


Install Stripe
- follow instruction on [stripeCLI](https://docs.stripe.com/stripe-cli/install?install-method=windows)
```bash
stripe login
```
